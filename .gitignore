# Python
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# Analysis outputs (each analysis has its own git repo)
analyses/

# Global memory (accumulates per-user, not shared)
memory/

# Development-only (not shipped to users)
tests/
docs/
